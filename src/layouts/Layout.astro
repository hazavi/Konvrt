<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" href="/konvrt_ico.ico" />
    <meta name="generator" content={Astro.generator} />
    <title>Konvrt — Media Converter</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,400;14..32,500;14..32,600;14..32,700;14..32,800&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  *,
  *::before,
  *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  :root {
    --bg: #06060b;
    --surface: #0e0e16;
    --surface-2: #141420;
    --surface-3: #1a1a28;
    --surface-hover: #1c1c2e;
    --border: #1e1e30;
    --border-hover: #2e2e48;
    --border-active: #4a4a66;
    --text: #eef0f6;
    --text-secondary: #a2a2c0;
    --text-dim: #5e5e7e;
    --accent: #7c6cf0;
    --accent-light: #a494ff;
    --accent-hover: #8d7ff5;
    --accent-glow: rgba(124, 108, 240, 0.1);
    --accent-glow-strong: rgba(124, 108, 240, 0.25);
    --success: #34d399;
    --success-bg: rgba(52, 211, 153, 0.08);
    --success-glow: rgba(52, 211, 153, 0.2);
    --error: #f87171;
    --error-bg: rgba(248, 113, 113, 0.08);
    --warning: #fbbf24;
    --video-color: #818cf8;
    --video-bg: rgba(129, 140, 248, 0.1);
    --audio-color: #fb923c;
    --audio-bg: rgba(251, 146, 60, 0.1);
    --image-color: #2dd4bf;
    --image-bg: rgba(45, 212, 191, 0.1);
    --gif-color: #f472b6;
    --gif-bg: rgba(244, 114, 182, 0.1);
    --pdf-color: #ef4444;
    --pdf-bg: rgba(239, 68, 68, 0.1);
    --radius: 12px;
    --radius-md: 8px;
    --radius-sm: 6px;
    --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.4);
    --shadow-md: 0 4px 16px rgba(0, 0, 0, 0.5);
    --shadow-lg: 0 12px 40px rgba(0, 0, 0, 0.6);
    --transition: 0.15s cubic-bezier(0.4, 0, 0.2, 1);
  }

  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
    font-family:
      "Inter",
      system-ui,
      -apple-system,
      sans-serif;
    background: var(--bg);
    color: var(--text);
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  ::-webkit-scrollbar {
    width: 4px;
    height: 4px;
  }
  ::-webkit-scrollbar-track {
    background: transparent;
  }
  ::-webkit-scrollbar-thumb {
    background: var(--border);
    border-radius: 4px;
  }
  ::-webkit-scrollbar-thumb:hover {
    background: var(--border-hover);
  }

  button {
    font-family: inherit;
    cursor: pointer;
  }
  select {
    font-family: inherit;
  }
  input {
    font-family: inherit;
  }

  /* Spinner keyframes for converting button */
  @keyframes spin {
    to {
      transform: rotate(360deg);
    }
  }
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(6px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  @keyframes successPop {
    0% {
      transform: scale(0.8);
      opacity: 0;
    }
    50% {
      transform: scale(1.15);
    }
    100% {
      transform: scale(1);
      opacity: 1;
    }
  }
  @keyframes progressGlow {
    0%,
    100% {
      box-shadow: 0 0 8px var(--accent-glow);
    }
    50% {
      box-shadow: 0 0 16px var(--accent-glow-strong);
    }
  }

  /* Toast notifications */
  @keyframes toastSlideIn {
    from {
      opacity: 0;
      transform: translateX(40px) scale(0.95);
    }
    to {
      opacity: 1;
      transform: translateX(0) scale(1);
    }
  }
  @keyframes toastSlideOut {
    from {
      opacity: 1;
      transform: translateX(0) scale(1);
    }
    to {
      opacity: 0;
      transform: translateX(40px) scale(0.95);
    }
  }
  .toast {
    pointer-events: auto;
    display: flex;
    align-items: flex-start;
    gap: 10px;
    padding: 12px 14px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.5), 0 2px 8px rgba(0, 0, 0, 0.3);
    animation: toastSlideIn 0.3s cubic-bezier(0.22, 1, 0.36, 1) forwards;
    min-width: 280px;
    max-width: 380px;
    backdrop-filter: blur(12px);
  }
  .toast.toast-exit {
    animation: toastSlideOut 0.25s cubic-bezier(0.4, 0, 1, 1) forwards;
  }
  .toast-icon {
    width: 28px;
    height: 28px;
    border-radius: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .toast-icon.success {
    background: rgba(52, 211, 153, 0.12);
    color: var(--success);
  }
  .toast-icon.error {
    background: rgba(248, 113, 113, 0.12);
    color: var(--error);
  }
  .toast-body {
    flex: 1;
    min-width: 0;
  }
  .toast-title {
    font-size: 12px;
    font-weight: 600;
    color: var(--text);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    line-height: 1.3;
  }
  .toast-meta {
    font-size: 10px;
    color: var(--text-dim);
    margin-top: 2px;
  }
  .toast-close {
    background: none;
    border: none;
    color: var(--text-dim);
    cursor: pointer;
    padding: 2px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 4px;
    transition: all var(--transition);
    flex-shrink: 0;
    opacity: 0.5;
  }
  .toast-close:hover {
    opacity: 1;
    color: var(--text);
    background: var(--surface-2);
  }
  .toast-progress {
    position: absolute;
    bottom: 0;
    left: 0;
    height: 2px;
    background: var(--accent);
    border-radius: 0 0 6px 6px;
    transition: width linear;
  }
  .toast.success {
    border-color: rgba(52, 211, 153, 0.2);
  }
  .toast.error {
    border-color: rgba(248, 113, 113, 0.2);
  }
</style>
