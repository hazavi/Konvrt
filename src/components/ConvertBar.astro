---

---

<div id="convert-bar" class="hidden">
  <div class="bar-inner">
    <div class="bar-left">
      <span id="stats-label" class="stats">0 files</span>
      <div class="bar-divider"></div>
      <button id="output-dir-btn" class="bar-chip" title="Select output folder">
        <svg
          width="13"
          height="13"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          ><path
            d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"
          ></path></svg
        >
        <span id="output-dir-label">Choose folder…</span>
      </button>
    </div>

    <div class="bar-center">
      <div class="bar-field" id="format-field">
        <div class="format-tabs" id="format-tabs">
          <button class="format-tab active" data-format-tab="video"
            >Video</button
          >
          <button class="format-tab" data-format-tab="audio">Audio</button>
        </div>
        <select id="format-select" class="field-select">
          <option value="">—</option>
        </select>
      </div>

      <div class="bar-field quality-field">
        <label class="field-label"
          >Quality <span id="quality-value" class="quality-val">80%</span
          ></label
        >
        <div class="slider-wrap">
          <input
            id="quality-slider"
            type="range"
            min="1"
            max="100"
            value="80"
            class="field-range"
          />
          <div class="slider-track">
            <div class="slider-fill" id="slider-fill" style="width:80%"></div>
          </div>
        </div>
        <button class="quality-help" id="quality-help" title="Quality guide">
          <svg
            width="12"
            height="12"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            ><circle cx="12" cy="12" r="10"></circle><path
              d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"></path><line
              x1="12"
              y1="17"
              x2="12.01"
              y2="17"></line></svg
          >
        </button>
        <div class="quality-tooltip" id="quality-tooltip">
          <div class="tooltip-arrow"></div>
          <div class="tooltip-title">Quality Guide</div>
          <div class="tooltip-row">
            <span class="tooltip-badge high">90-100%</span> Best quality, larger file
          </div>
          <div class="tooltip-row">
            <span class="tooltip-badge good">70-89%</span> Recommended balance
          </div>
          <div class="tooltip-row">
            <span class="tooltip-badge medium">40-69%</span> Smaller file, some loss
          </div>
          <div class="tooltip-row">
            <span class="tooltip-badge low">1-39%</span> Smallest file, noticeable
            loss
          </div>
        </div>
      </div>

      <div class="bar-field" id="mode-field">
        <label class="field-label">Mode</label>
        <div class="mode-toggle" id="mode-toggle">
          <button class="mode-btn active" data-mode="convert">Convert</button>
          <button class="mode-btn" data-mode="compress">Compress</button>
        </div>
      </div>
    </div>

    <div class="bar-right">
      <button id="clear-btn" class="btn-secondary" style="display:none;"
        >Clear</button
      >
      <button id="convert-btn" class="btn-primary" disabled>
        <svg
          width="14"
          height="14"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2.5"
          stroke-linecap="round"
          stroke-linejoin="round"
          ><polyline points="16 3 21 3 21 8"></polyline><line
            x1="4"
            y1="20"
            x2="21"
            y2="3"></line></svg
        >
        Convert All
      </button>
    </div>
  </div>

  <!-- Overall progress -->
  <div id="overall-progress" class="overall-progress" style="display:none;">
    <div class="overall-bar">
      <div id="overall-fill" class="overall-fill" style="width:0%"></div>
    </div>
    <span id="overall-text" class="overall-text">0%</span>
  </div>
</div>

<style>
  #convert-bar {
    border-top: 1px solid var(--border);
    background: linear-gradient(
      180deg,
      rgba(14, 14, 22, 0.96) 0%,
      rgba(6, 6, 11, 0.99) 100%
    );
    backdrop-filter: blur(16px);
    padding: 14px 20px;
    flex-shrink: 0;
    transition:
      transform 0.3s cubic-bezier(0.4, 0, 0.2, 1),
      opacity 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
  }
  #convert-bar::before {
    content: "";
    position: absolute;
    top: -1px;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(
      90deg,
      transparent 0%,
      var(--accent-glow) 50%,
      transparent 100%
    );
    pointer-events: none;
  }
  #convert-bar.hidden {
    transform: translateY(100%);
    opacity: 0;
    pointer-events: none;
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
  }

  .bar-inner {
    display: flex;
    align-items: center;
    gap: 14px;
  }

  .bar-left {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-shrink: 0;
  }
  .stats {
    font-size: 11px;
    color: var(--text-dim);
    font-weight: 500;
    white-space: nowrap;
    font-variant-numeric: tabular-nums;
  }
  .bar-divider {
    width: 1px;
    height: 18px;
    background: var(--border);
  }
  .bar-chip {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    padding: 4px 10px;
    border: 1px solid var(--border);
    border-radius: 6px;
    background: var(--surface-2);
    color: var(--text-dim);
    font-size: 10.5px;
    cursor: pointer;
    transition: all var(--transition);
    max-width: 180px;
  }
  .bar-chip span {
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .bar-chip:hover {
    border-color: var(--accent);
    color: var(--text);
    background: rgba(124, 108, 240, 0.06);
  }

  .bar-center {
    flex: 1;
    display: flex;
    align-items: center;
    gap: 14px;
    justify-content: center;
  }
  .bar-field {
    display: flex;
    align-items: center;
    gap: 6px;
  }
  .field-label {
    font-size: 9.5px;
    font-weight: 600;
    text-transform: uppercase;
    color: var(--text-dim);
    letter-spacing: 0.6px;
    white-space: nowrap;
  }
  .quality-val {
    color: var(--accent-light);
    font-weight: 700;
    font-variant-numeric: tabular-nums;
  }

  .field-select {
    padding: 4px 8px;
    border: 1px solid var(--border);
    border-radius: 6px;
    background: var(--surface-2);
    color: var(--text);
    font-size: 11.5px;
    font-weight: 600;
    cursor: pointer;
    outline: none;
    transition: border-color var(--transition);
  }
  .field-select:focus {
    border-color: var(--accent);
    box-shadow: 0 0 0 2px var(--accent-glow);
  }
  .field-select optgroup {
    font-weight: 600;
    color: var(--text-dim);
    font-size: 10px;
  }
  .field-select option {
    color: var(--text);
    background: var(--surface-2);
  }

  /* Custom quality slider */
  .quality-field {
    position: relative;
  }
  .slider-wrap {
    position: relative;
    width: 100px;
    height: 16px;
    display: flex;
    align-items: center;
  }
  .field-range {
    width: 100%;
    height: 16px;
    cursor: pointer;
    -webkit-appearance: none;
    appearance: none;
    background: transparent;
    position: relative;
    z-index: 2;
    margin: 0;
  }
  .field-range::-webkit-slider-runnable-track {
    height: 4px;
    background: transparent;
    border-radius: 2px;
  }
  .field-range::-webkit-slider-thumb {
    -webkit-appearance: none;
    width: 14px;
    height: 14px;
    border-radius: 50%;
    background: white;
    border: 2px solid var(--accent);
    box-shadow: 0 1px 4px rgba(0, 0, 0, 0.3);
    margin-top: -5px;
    cursor: pointer;
    transition:
      box-shadow 0.15s,
      transform 0.15s;
  }
  .field-range::-webkit-slider-thumb:hover {
    box-shadow:
      0 0 0 4px var(--accent-glow),
      0 1px 4px rgba(0, 0, 0, 0.3);
    transform: scale(1.1);
  }
  .field-range:active::-webkit-slider-thumb {
    box-shadow:
      0 0 0 6px var(--accent-glow-strong),
      0 1px 4px rgba(0, 0, 0, 0.3);
  }
  .slider-track {
    position: absolute;
    top: 50%;
    left: 0;
    right: 0;
    height: 4px;
    background: var(--border);
    border-radius: 2px;
    transform: translateY(-50%);
    pointer-events: none;
    z-index: 1;
    overflow: hidden;
  }
  .slider-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--accent), var(--accent-light));
    border-radius: 2px;
    transition: width 0.05s linear;
  }

  /* Quality help button */
  .quality-help {
    width: 20px;
    height: 20px;
    border: 1px solid var(--border);
    border-radius: 50%;
    background: var(--surface-2);
    color: var(--text-dim);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all var(--transition);
    flex-shrink: 0;
    padding: 0;
  }
  .quality-help:hover {
    border-color: var(--accent);
    color: var(--accent-light);
    background: var(--accent-glow);
  }

  /* Quality tooltip */
  .quality-tooltip {
    display: none;
    position: absolute;
    bottom: calc(100% + 10px);
    left: 50%;
    transform: translateX(-50%);
    background: var(--surface-3);
    border: 1px solid var(--border-hover);
    border-radius: var(--radius-md);
    padding: 10px 12px;
    min-width: 220px;
    z-index: 100;
    box-shadow: var(--shadow-lg);
    animation: fadeIn 0.15s ease-out;
  }
  .quality-tooltip.visible {
    display: block;
  }
  .tooltip-arrow {
    position: absolute;
    bottom: -5px;
    left: 50%;
    transform: translateX(-50%) rotate(45deg);
    width: 8px;
    height: 8px;
    background: var(--surface-3);
    border-right: 1px solid var(--border-hover);
    border-bottom: 1px solid var(--border-hover);
  }
  .tooltip-title {
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    color: var(--text-secondary);
    letter-spacing: 0.5px;
    margin-bottom: 6px;
  }
  .tooltip-row {
    font-size: 10.5px;
    color: var(--text-dim);
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 2px 0;
  }
  .tooltip-badge {
    font-size: 9px;
    font-weight: 700;
    padding: 1px 5px;
    border-radius: 3px;
    flex-shrink: 0;
    font-variant-numeric: tabular-nums;
  }
  .tooltip-badge.high {
    background: rgba(52, 211, 153, 0.12);
    color: #34d399;
  }
  .tooltip-badge.good {
    background: rgba(124, 108, 240, 0.12);
    color: var(--accent-light);
  }
  .tooltip-badge.medium {
    background: rgba(251, 191, 36, 0.12);
    color: #fbbf24;
  }
  .tooltip-badge.low {
    background: rgba(248, 113, 113, 0.12);
    color: #f87171;
  }

  /* Format tabs (Video / Audio) for video files */
  .format-tabs {
    display: none;
    gap: 1px;
    padding: 1px;
    background: var(--surface-2);
    border: 1px solid var(--border);
    border-radius: 5px;
  }
  .format-tabs.visible {
    display: flex;
  }
  :global(.format-tab) {
    padding: 2px 8px;
    border: none;
    border-radius: 4px;
    background: transparent;
    color: var(--text-dim);
    font-size: 9.5px;
    font-weight: 600;
    cursor: pointer;
    transition: all var(--transition);
    text-transform: uppercase;
    letter-spacing: 0.3px;
  }
  :global(.format-tab:hover) {
    color: var(--text-secondary);
  }
  :global(.format-tab.active) {
    background: var(--accent);
    color: white;
    box-shadow: 0 1px 3px var(--accent-glow);
  }

  .mode-toggle {
    display: flex;
    background: var(--surface-2);
    border: 1px solid var(--border);
    border-radius: 6px;
    overflow: hidden;
    gap: 1px;
    padding: 1px;
  }
  .mode-btn {
    padding: 3px 10px;
    border: none;
    border-radius: 5px;
    background: transparent;
    color: var(--text-dim);
    font-size: 10.5px;
    font-weight: 600;
    cursor: pointer;
    transition: all var(--transition);
  }
  .mode-btn.active {
    background: var(--accent);
    color: white;
    box-shadow: 0 1px 4px var(--accent-glow);
  }
  .mode-btn:hover:not(.active) {
    color: var(--text);
  }

  .bar-right {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-shrink: 0;
  }

  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 7px 20px;
    border: none;
    border-radius: var(--radius-md);
    background: linear-gradient(135deg, var(--accent) 0%, #6c5ce7 100%);
    color: #fff;
    font-size: 12.5px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 2px 10px var(--accent-glow);
    position: relative;
    overflow: hidden;
  }
  .btn-primary::before {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(
      135deg,
      rgba(255, 255, 255, 0.1) 0%,
      transparent 50%
    );
    pointer-events: none;
  }
  .btn-primary:hover:not(:disabled) {
    background: linear-gradient(
      135deg,
      var(--accent-hover) 0%,
      var(--accent-light) 100%
    );
    box-shadow: 0 4px 20px var(--accent-glow-strong);
    transform: translateY(-1px);
  }
  .btn-primary:active:not(:disabled) {
    transform: translateY(0);
    box-shadow: 0 1px 6px var(--accent-glow);
  }
  .btn-primary:disabled {
    opacity: 0.35;
    cursor: not-allowed;
    transform: none;
  }
  /* Converting state */
  :global(.btn-primary.converting) {
    background: linear-gradient(135deg, var(--accent) 0%, #6c5ce7 100%);
    pointer-events: none;
    animation: progressGlow 2s ease-in-out infinite;
  }
  /* Done state */
  :global(.btn-primary.done) {
    background: linear-gradient(135deg, #059669 0%, #34d399 100%);
    box-shadow: 0 2px 12px var(--success-glow);
    animation: successPop 0.4s ease-out;
  }
  :global(.btn-primary.done:hover) {
    background: linear-gradient(135deg, #047857 0%, #2cc586 100%);
    box-shadow: 0 4px 20px var(--success-glow);
    transform: translateY(-1px);
  }
  /* Spinner inside button */
  :global(.btn-spinner) {
    width: 14px;
    height: 14px;
    border: 2px solid rgba(255, 255, 255, 0.3);
    border-top-color: white;
    border-radius: 50%;
    animation: spin 0.7s linear infinite;
    flex-shrink: 0;
  }

  .btn-secondary {
    padding: 6px 14px;
    border: 1px solid var(--border);
    border-radius: 6px;
    background: transparent;
    color: var(--text-dim);
    font-size: 11px;
    font-weight: 500;
    cursor: pointer;
    transition: all var(--transition);
  }
  .btn-secondary:hover {
    border-color: var(--error);
    color: var(--error);
    background: var(--error-bg);
  }

  .overall-progress {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-top: 8px;
  }
  .overall-bar {
    flex: 1;
    height: 2px;
    background: var(--border);
    border-radius: 2px;
    overflow: hidden;
  }
  .overall-fill {
    height: 100%;
    background: linear-gradient(
      90deg,
      var(--accent),
      var(--accent-light),
      var(--success)
    );
    border-radius: 2px;
    transition: width 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 0 8px var(--accent-glow);
  }
  .overall-text {
    font-size: 10.5px;
    font-weight: 700;
    color: var(--accent-light);
    min-width: 30px;
    text-align: right;
    font-variant-numeric: tabular-nums;
  }
</style>
